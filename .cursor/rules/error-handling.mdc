---
description: Backend and frontend error handling policy
alwaysApply: true
---

- Backend errors must map to explicit HTTP status codes and JSON error responses.
- Use Nest exceptions (`BadRequestException`, `InternalServerErrorException`) for expected failures.
- Frontend should surface API failures with user-visible toast messages.
- Never swallow exceptions silently.
